package com.lmy.codec.texture.impl.sticker

class TextSticker(frameBuffer: IntArray,
                  name: String = "BaseSticker") : BaseSticker(frameBuffer, name) {

    override fun draw(transformMatrix: FloatArray?) {

    }
}